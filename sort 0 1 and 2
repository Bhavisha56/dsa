tc O(nlogn) sc O(1)

class Solution {
    // Function to sort an array of 0s, 1s, and 2s
    public void sort012(ArrayList<Integer> arr) {
        // code here
        Collections.sort(arr);
    }
}

tc O(n) sc O(1)
dutch national flag
set low=0 mid=0 high =n-1;
and check mid==0 -> swap (mid,low) mid++;low++
         mid==1 ->  mid++;
         mid==2 -> high--;
